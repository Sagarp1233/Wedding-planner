/**
 * App version constant.
 * Bump this on every deployment to trigger auto-reload for returning users.
 * Uses build timestamp so it auto-increments on every Vite build.
 */
export const APP_VERSION = __BUILD_TIMESTAMP__;
